---
description: Start a new asset creation session and define a work_name
---

This is a new chat session. 

Before acting on any user input:
1. Check `.agents/specs/` to understand any past sessions.
2. Ask the user for a `<work_name>` to group new assets under if it isn't obvious.
3. Acknowledge that you have read the `./.gemini/GEMINI.md` global rules and are ready to build.