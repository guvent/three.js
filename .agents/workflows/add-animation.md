---
description: Add a new reaction or state animation to an existing asset
---

Activate the **Animator** role.

I want to add a new animation clip (e.g. hit, die, jump, celebrate) to an existing character or prop. Please follow the Animator rules in `./.gemini/GEMINI.md` to create the clip, set the correct loop behavior (`LoopOnce` vs `LoopRepeat`), and update the existing asset's `design.md` spec file.